'use client'
import React from 'react';

const ProductCard = () => {
    return (
        <div>
            <button onClick={() => console.log('click event')}>Add to Cart</button>
        </div>
    )
}

export default ProductCard;